<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users List - Secure App</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        .navbar {
            background-color: #17a2b8;
            padding: 15px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            border-radius: 4px;
        }

        .navbar h3 {
            color: white;
            margin: 0;
        }

        .nav-links {
            display: flex;
            gap: 15px;
        }

        .nav-links a {
            color: white;
            text-decoration: none;
            padding: 8px 12px;
            border-radius: 4px;
            background-color: rgba(255, 255, 255, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.3);
            transition: background-color 0.3s;
            font-weight: 500;
        }

        .nav-links a:hover {
            background-color: rgba(255, 255, 255, 0.3);
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        table thead {
            background-color: #17a2b8;
            color: white;
        }

        table th,
        table td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        table tr:hover {
            background-color: #f5f5f5;
        }

        .info-text {
            color: #666;
            font-size: 14px;
            margin-top: 15px;
        }
    </style>
</head>

<body>
    <div class="navbar">
        <h3>👥 Users Directory</h3>
        <div class="nav-links">
            <a href="/profile">Back to Profile</a>
            <a href="/logout">Logout</a>
        </div>
    </div>

    <div class="container">
        <h2>📋 Registered Users</h2>

        <table>
            <thead>
                <tr>
                    <th>Username</th>
                    <th>Email</th>
                    <th>Role</th>
                </tr>
            </thead>
            <tbody>
                <% users.forEach(user=> { %>
                    <tr>
                        <td><strong>
                                <%= user.username %>
                            </strong></td>
                        <td>
                            <%= user.email %>
                        </td>
                        <td>
                            <%= user.isAdmin %>
                        </td>
                    </tr>
                    <% }); %>
            </tbody>
        </table>

        <div class="info-text">
            <strong>ℹ️ Total Users:</strong>
            <%= users.length %>
        </div>
    </div>
</body>

</html>