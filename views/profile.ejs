<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile - Secure App</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        .navbar {
            background-color: #007bff;
            padding: 15px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            border-radius: 4px;
        }

        .navbar h3 {
            color: white;
            margin: 0;
        }

        .nav-links {
            display: flex;
            gap: 15px;
        }

        .nav-links a,
        .nav-links button {
            color: white;
            text-decoration: none;
            padding: 8px 12px;
            border-radius: 4px;
            background-color: rgba(255, 255, 255, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.3);
            cursor: pointer;
            transition: background-color 0.3s;
            font-weight: 500;
        }

        .nav-links a:hover,
        .nav-links button:hover {
            background-color: rgba(255, 255, 255, 0.3);
        }

        .profile-card {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            border-left: 4px solid #28a745;
            margin-bottom: 20px;
        }

        .profile-field {
            margin-bottom: 15px;
        }

        .profile-field label {
            font-weight: 600;
            color: #333;
        }

        .profile-field value {
            display: block;
            margin-top: 5px;
            padding: 8px 12px;
            background-color: white;
            border: 1px solid #ddd;
            border-radius: 4px;
            font-family: monospace;
        }

        .badge {
            display: inline-block;
            padding: 4px 8px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
        }

        .badge.admin {
            background-color: #ffc107;
            color: #333;
        }

        .badge.user {
            background-color: #17a2b8;
            color: white;
        }

        .info-box {
            background-color: #e7f3ff;
            padding: 15px;
            border-radius: 4px;
            border-left: 4px solid #2196F3;
            margin-top: 20px;
            color: #1565c0;
        }
    </style>
</head>

<body>
    <div class="navbar">
        <h3>🔐 Secure Dashboard</h3>
        <div class="nav-links">
            <a href="/users">View Users</a>
            <% if (isAdmin) { %>
                <a href="/admin">Admin Panel</a>
                <% } %>
                    <a href="/logout">Logout</a>
        </div>
    </div>

    <div class="container">
        <h2>👤 User Profile</h2>

        <div class="profile-card">
            <div class="profile-field">
                <label>Username:</label>
                <value>
                    <%= user %>
                </value>
            </div>
            <div class="profile-field">
                <label>Email:</label>
                <value>
                    <%= email %>
                </value>
            </div>
            <div class="profile-field">
                <label>User ID:</label>
                <value>
                    <%= userId %>
                </value>
            </div>
            <div class="profile-field">
                <label>Role:</label>
                <% if (isAdmin) { %>
                    <span class="badge admin">ADMIN</span>
                    <% } else { %>
                        <span class="badge user">USER</span>
                        <% } %>
            </div>
        </div>

        <div class="info-box">
            <strong>Security Information:</strong>
            <p style="margin: 10px 0 0 0;">
                ✓ Your password is securely hashed with bcrypt<br>
                ✓ Your session is protected with JWT tokens<br>
                ✓ All communications are secured with Helmet.js<br>
                ✓ All inputs are validated and sanitized
            </p>
        </div>
    </div>
</body>

</html>