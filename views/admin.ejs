<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel - Secure App</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        .navbar {
            background-color: #dc3545;
            padding: 15px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            border-radius: 4px;
        }

        .navbar h3 {
            color: white;
            margin: 0;
        }

        .nav-links {
            display: flex;
            gap: 15px;
        }

        .nav-links a {
            color: white;
            text-decoration: none;
            padding: 8px 12px;
            border-radius: 4px;
            background-color: rgba(255, 255, 255, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.3);
            transition: background-color 0.3s;
            font-weight: 500;
        }

        .nav-links a:hover {
            background-color: rgba(255, 255, 255, 0.3);
        }

        .admin-warning {
            background-color: #fff3cd;
            border: 1px solid #ffeaa7;
            padding: 15px;
            border-radius: 4px;
            margin-bottom: 20px;
            color: #856404;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        table thead {
            background-color: #dc3545;
            color: white;
        }

        table th,
        table td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        table tr:hover {
            background-color: #f5f5f5;
        }

        .badge {
            display: inline-block;
            padding: 4px 8px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
        }

        .badge.admin {
            background-color: #ffc107;
            color: #333;
        }

        .badge.user {
            background-color: #17a2b8;
            color: white;
        }
    </style>
</head>

<body>
    <div class="navbar">
        <h3>⚙️ Admin Panel</h3>
        <div class="nav-links">
            <a href="/profile">Back to Profile</a>
            <a href="/logout">Logout</a>
        </div>
    </div>

    <div class="container">
        <h2>📊 System Administration</h2>

        <div class="admin-warning">
            <strong>⚠️ Restricted Access:</strong> This page is only accessible to administrators. All access is logged
            and monitored.
        </div>

        <h3>All Users (<%= users.length %>)</h3>
        <table>
            <thead>
                <tr>
                    <th>User ID</th>
                    <th>Username</th>
                    <th>Email</th>
                    <th>Role</th>
                </tr>
            </thead>
            <tbody>
                <% users.forEach(user=> { %>
                    <tr>
                        <td>
                            <%= user.id %>
                        </td>
                        <td><strong>
                                <%= user.username %>
                            </strong></td>
                        <td>
                            <%= user.email %>
                        </td>
                        <td>
                            <% if (user.isAdmin) { %>
                                <span class="badge admin">ADMIN</span>
                                <% } else { %>
                                    <span class="badge user">USER</span>
                                    <% } %>
                        </td>
                    </tr>
                    <% }); %>
            </tbody>
        </table>
    </div>
</body>

</html>